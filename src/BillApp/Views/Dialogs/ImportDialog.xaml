<Window x:Class="BillApp.Views.Dialogs.ImportDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Import Data"
        Width="600"
        Height="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        MinWidth="500"
        MinHeight="400"
        ShowInTaskbar="False">
    <Window.Resources>
        <Style TargetType="TextBlock" x:Key="LabelStyle">
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>

    <Grid>
        <!-- Loading Overlay -->
        <Border x:Name="LoadingOverlay"
                Background="#CC000000"
                Visibility="Collapsed"
                Panel.ZIndex="100">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True"
                             Width="200"
                             Height="20"
                             Margin="0,0,0,15"/>
                <TextBlock x:Name="LoadingText"
                           Text="Importing data..."
                           Foreground="White"
                           FontSize="16"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="Please wait, this may take a moment."
                           Foreground="#CCCCCC"
                           FontSize="12"
                           HorizontalAlignment="Center"
                           Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- File Selection -->
        <TextBlock Grid.Row="0" Text="CSV File:" Style="{StaticResource LabelStyle}"/>
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="FilePathBox"
                     Grid.Column="0"
                     IsReadOnly="True"
                     Padding="8,6"
                     VerticalContentAlignment="Center"/>
            <Button Grid.Column="1"
                    Content="Browse..."
                    Padding="15,6"
                    Margin="10,0,0,0"
                    Click="BrowseButton_Click"/>
        </Grid>

        <!-- Import Mode -->
        <TextBlock Grid.Row="2" Text="Import Mode:" Style="{StaticResource LabelStyle}"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="100,0,0,15">
            <RadioButton x:Name="AddToExistingRadio"
                         Content="Add to existing data"
                         IsChecked="True"
                         Margin="0,0,20,0"/>
            <RadioButton x:Name="ReplaceAllRadio"
                         Content="Replace all data (deletes existing)"/>
        </StackPanel>

        <!-- Validation Results -->
        <TextBlock Grid.Row="3" Text="Validation Results:" Style="{StaticResource LabelStyle}" VerticalAlignment="Top"/>
        <Border Grid.Row="3" Margin="0,25,0,0" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4">
            <ListBox x:Name="ValidationList"
                     BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="5,3">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="60"/>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding RowDisplay}" Foreground="Gray" FontSize="11"/>
                            <TextBlock Grid.Column="1" FontWeight="SemiBold" FontSize="11">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="{Binding Severity}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Severity}" Value="Error">
                                                <Setter Property="Foreground" Value="#D32F2F"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Severity}" Value="Warning">
                                                <Setter Property="Foreground" Value="#F57C00"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Severity}" Value="Info">
                                                <Setter Property="Foreground" Value="#1976D2"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            <TextBlock Grid.Column="2" Text="{Binding Message}" TextWrapping="Wrap"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <!-- Summary -->
        <Border Grid.Row="4" Background="#F5F5F5" Padding="10" Margin="0,15,0,0" CornerRadius="4">
            <TextBlock x:Name="SummaryText" TextWrapping="Wrap"/>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="ImportButton"
                    Content="Import"
                    Width="100"
                    Padding="8,6"
                    Margin="0,0,10,0"
                    IsEnabled="False"
                    Click="ImportButton_Click"/>
            <Button Content="Cancel"
                    Width="100"
                    Padding="8,6"
                    IsCancel="True"
                    Click="CancelButton_Click"/>
        </StackPanel>
        </Grid>
    </Grid>
</Window>
