<UserControl x:Class="BillApp.Views.Investments.InvestmentsOverviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:BillApp.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:InvestmentsOverviewViewModel}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="3"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Investments"
                       FontSize="18"
                       FontWeight="Bold"
                       Margin="0,0,0,12"
                       Grid.ColumnSpan="3"/>

            <StackPanel Orientation="Vertical">
                <Button Content="Quick Import"
                        Command="{Binding QuickImportCommand}"
                        Padding="16,10"/>
                <TextBlock Text="{Binding StatusMessage}"
                           Margin="0,8,0,0"
                           Foreground="Gray"
                           TextWrapping="Wrap"
                           Width="220"/>
            </StackPanel>

            <Border Grid.Column="2"
                    Padding="16"
                    Background="#FFF"
                    BorderBrush="#DDD"
                    BorderThickness="1"
                    CornerRadius="4">
                <StackPanel>
                    <TextBlock Text="Add Individual Holding"
                               FontWeight="SemiBold"
                               Margin="0,0,0,8"/>
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Account" Margin="0,0,8,4" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding NewAccountName, UpdateSourceTrigger=PropertyChanged}" Padding="6,4"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Symbol" Margin="0,0,8,4" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding NewSymbol, UpdateSourceTrigger=PropertyChanged}" Padding="6,4"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Description" Margin="0,0,8,4" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding NewDescription, UpdateSourceTrigger=PropertyChanged}" Padding="6,4"/>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="Quantity" Margin="0,0,8,4" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding NewQuantity, UpdateSourceTrigger=PropertyChanged}" Padding="6,4"/>

                        <TextBlock Grid.Row="4" Grid.Column="0" Text="Price" Margin="0,0,8,4" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding NewPrice, UpdateSourceTrigger=PropertyChanged}" Padding="6,4"/>

                        <TextBlock Grid.Row="5" Grid.Column="0" Text="Market Value" Margin="0,0,8,4" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding NewMarketValue, UpdateSourceTrigger=PropertyChanged}" Padding="6,4"/>

                        <TextBlock Grid.Row="6" Grid.Column="0" Text="Asset Class" Margin="0,0,8,4" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="6" Grid.Column="1" Text="{Binding NewAssetClass, UpdateSourceTrigger=PropertyChanged}" Padding="6,4"/>

                        <TextBlock Grid.Row="7" Grid.Column="0" Text="% of Account" Margin="0,0,8,4" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="7" Grid.Column="1" Text="{Binding NewPercentOfAccount, UpdateSourceTrigger=PropertyChanged}" Padding="6,4"/>
                    </Grid>

                    <Button Content="Add Holding"
                            Command="{Binding AddHoldingCommand}"
                            Padding="12,6"
                            HorizontalAlignment="Left"/>
                </StackPanel>
            </Border>
        </Grid>

        <GroupBox Header="Snapshots" Grid.Row="1">
            <DataGrid ItemsSource="{Binding Snapshots}"
                      SelectedItem="{Binding SelectedSnapshot}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      Margin="0,8,0,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Imported" Binding="{Binding ImportedAt, StringFormat={}{0:g}}"/>
                    <DataGridTextColumn Header="Source" Binding="{Binding SourceName}"/>
                    <DataGridTextColumn Header="Holdings" Binding="{Binding Holdings.Count}"/>
                    <DataGridTextColumn Header="Total Value" Binding="{Binding TotalValue, StringFormat={}{0:C2}}"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <GroupBox Header="Selected Snapshot Holdings" Grid.Row="2" Margin="0,16,0,0">
            <StackPanel>
                <TextBlock Text="{Binding SelectedSnapshotTotal, StringFormat='Snapshot total: {0:C2}'}"
                           FontWeight="SemiBold"
                           Margin="0,0,0,8"/>
                <DataGrid ItemsSource="{Binding SnapshotHoldings}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          MaxHeight="360">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Account" Binding="{Binding AccountName}"/>
                        <DataGridTextColumn Header="Symbol" Binding="{Binding Symbol}"/>
                        <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="2*"/>
                        <DataGridTextColumn Header="Qty" Binding="{Binding Quantity, StringFormat={}{0:N4}}"/>
                        <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat={}{0:C2}}"/>
                        <DataGridTextColumn Header="Value" Binding="{Binding MarketValue, StringFormat={}{0:C2}}"/>
                        <DataGridTextColumn Header="Asset Class" Binding="{Binding AssetClass}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </GroupBox>
    </Grid>
</UserControl>
