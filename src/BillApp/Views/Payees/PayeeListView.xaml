<UserControl x:Class="BillApp.Views.Payees.PayeeListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:BillApp.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:PayeeListViewModel}"
             d:DesignHeight="600" d:DesignWidth="1000">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header with Title and Add Button -->
        <Grid Grid.Row="0" Margin="20,20,20,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Accounts" FontSize="24" FontWeight="Bold"/>
            <Button Grid.Column="1"
                    Content="+ Add Account"
                    Command="{Binding AddPayeeCommand}"
                    Padding="15,8"/>
        </Grid>

        <!-- Filters and Summary -->
        <Grid Grid.Row="1" Margin="20,0,20,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Filters -->
            <StackPanel Orientation="Horizontal" Grid.Column="0">
                <TextBox Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"
                         Width="200"
                         Padding="8,6"
                         Margin="0,0,10,0">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding ApplyFilterCommand}"/>
                    </TextBox.InputBindings>
                </TextBox>

                <ComboBox ItemsSource="{Binding TypeFilterOptions}"
                          SelectedItem="{Binding FilterType}"
                          Width="150"
                          Padding="8,6"
                          Margin="0,0,10,0"/>

                <Button Content="Apply"
                        Command="{Binding ApplyFilterCommand}"
                        Padding="12,6"
                        Margin="0,0,5,0"/>

                <Button Content="Clear"
                        Command="{Binding ClearFilterCommand}"
                        Padding="12,6"
                        Margin="0,0,15,0"/>

                <CheckBox Content="Show Inactive"
                          IsChecked="{Binding ShowInactive}"
                          VerticalAlignment="Center"
                          Command="{Binding ApplyFilterCommand}"/>
            </StackPanel>

            <!-- Account Summary -->
            <Border Grid.Column="1" Background="#F5F5F5" Padding="15,8">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Assets:" FontWeight="SemiBold" Margin="0,0,5,0"/>
                <TextBlock Text="{Binding TotalAssets, StringFormat='{}{0:C}'}"
                           Foreground="Green" Margin="0,0,15,0"/>
                <TextBlock Text="Liabilities:" FontWeight="SemiBold" Margin="0,0,5,0"/>
                <TextBlock Text="{Binding TotalLiabilities, StringFormat='{}{0:C}'}"
                           Foreground="Red" Margin="0,0,15,0"/>
                <TextBlock Text="Net Worth:" FontWeight="SemiBold" Margin="0,0,5,0"/>
                <TextBlock Text="{Binding NetWorth, StringFormat='{}{0:C}'}"
                           FontWeight="Bold"/>
            </StackPanel>
            </Border>
        </Grid>

        <!-- Payee List -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Payees}"
                  SelectedItem="{Binding SelectedPayee}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  Margin="20,0,20,20"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  BorderThickness="1"
                  BorderBrush="#DDDDDD">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category.Name}" Width="100"/>
                <DataGridTextColumn Header="Type" Width="80">
                    <DataGridTextColumn.Binding>
                        <Binding Path="IsAccount">
                            <Binding.Converter>
                                <local:BoolToStringConverter xmlns:local="clr-namespace:BillApp.Converters"
                                                             TrueValue="Account"
                                                             FalseValue="Payee"/>
                            </Binding.Converter>
                        </Binding>
                    </DataGridTextColumn.Binding>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Account Type" Binding="{Binding AccountType}" Width="100"/>
                <DataGridTemplateColumn Header="Balance" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Balance, StringFormat='{}{0:C}'}"
                                       HorizontalAlignment="Right"
                                       Padding="0,0,5,0">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <!-- Hide balance for non-financial accounts (not asset and not liability) -->
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding IsAsset}" Value="False"/>
                                                    <Condition Binding="{Binding IsLiability}" Value="False"/>
                                                </MultiDataTrigger.Conditions>
                                                <Setter Property="Visibility" Value="Hidden"/>
                                            </MultiDataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Last Paid" Binding="{Binding LastPaidDate, StringFormat='{}{0:d}'}" Width="90"/>
                <DataGridTextColumn Header="Institution" Binding="{Binding Institution}" Width="120"/>
                <DataGridCheckBoxColumn Header="Payment" Binding="{Binding IsPaymentAccount, Mode=OneWay}" Width="60"/>
                <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive, Mode=OneWay}" Width="50"/>
                <DataGridTemplateColumn Header="Actions" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Edit"
                                        Command="{Binding DataContext.EditPayeeCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Padding="8,4"
                                        Margin="0,0,5,0"/>
                                <Button Content="Delete"
                                        Command="{Binding DataContext.DeletePayeeCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Padding="8,4"
                                        Foreground="Red"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>

            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsActive}" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </DataTrigger>
                    </Style.Triggers>
                    <EventSetter Event="MouseDoubleClick" Handler="DataGridRow_DoubleClick"/>
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>

        <!-- Loading Indicator -->
        <Border Grid.Row="2"
                Background="#88FFFFFF"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <TextBlock Text="Loading..."
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="18"/>
        </Border>

        <!-- Error Message -->
        <Border Grid.Row="2"
                Background="#FFEBEE"
                Padding="20"
                Margin="20"
                CornerRadius="4"
                Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}"
                VerticalAlignment="Top">
            <TextBlock Text="{Binding ErrorMessage}"
                       Foreground="#C62828"
                       TextWrapping="Wrap"/>
        </Border>
    </Grid>
</UserControl>
