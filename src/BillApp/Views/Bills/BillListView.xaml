<UserControl x:Class="BillApp.Views.Bills.BillListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:BillApp.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:BillListViewModel}"
             d:DesignHeight="600" d:DesignWidth="900">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Bills" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Quick Add..."
                        Command="{Binding QuickAddBillsCommand}"
                        Padding="15,8"
                        Margin="0,0,10,0"
                        Background="#FF9800"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        ToolTip="Quickly add multiple bills from pasted text"/>
                <Button Content="+ Add Bill"
                        Command="{Binding AddBillCommand}"
                        Padding="15,8"
                        Background="#0078D4"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"/>
            </StackPanel>
        </DockPanel>

        <!-- Filters -->
        <Border Grid.Row="1" Background="#F5F5F5" Padding="15" Margin="0,0,0,15" CornerRadius="4">
            <StackPanel Orientation="Horizontal">
                <TextBox Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"
                         Width="200"
                         Padding="8"
                         VerticalContentAlignment="Center">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <VisualBrush Stretch="None" AlignmentX="Left">
                                                <VisualBrush.Visual>
                                                    <TextBlock Text="Search payee..." Foreground="Gray" Margin="8,0,0,0"/>
                                                </VisualBrush.Visual>
                                            </VisualBrush>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <ComboBox ItemsSource="{Binding StatusFilterOptions}"
                          SelectedItem="{Binding FilterStatus}"
                          Width="120"
                          Margin="10,0,0,0"
                          Padding="8"/>

                <ComboBox ItemsSource="{Binding Categories}"
                          SelectedValue="{Binding FilterCategoryId}"
                          SelectedValuePath="Id"
                          DisplayMemberPath="Name"
                          Width="150"
                          Margin="10,0,0,0"
                          Padding="8"/>

                <Button Content="Apply"
                        Command="{Binding ApplyFilterCommand}"
                        Margin="10,0,0,0"
                        Padding="15,8"/>

                <Button Content="Clear"
                        Command="{Binding ClearFilterCommand}"
                        Margin="5,0,0,0"
                        Padding="15,8"/>
            </StackPanel>
        </Border>

        <!-- Bills DataGrid -->
        <DataGrid x:Name="BillsDataGrid"
                  Grid.Row="2"
                  ItemsSource="{Binding Bills}"
                  SelectedItem="{Binding SelectedBill}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserSortColumns="True"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  BorderThickness="1"
                  BorderBrush="#E0E0E0"
                  CellEditEnding="DataGrid_CellEditEnding">

            <DataGrid.Columns>
                <!-- Payee -->
                <DataGridTemplateColumn Header="Payee" Width="*" MinWidth="120" SortMemberPath="Payee.Name">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Payee.Name}" VerticalAlignment="Center"/>
                                <Border Margin="6,0,0,0"
                                        Padding="2"
                                        CornerRadius="2"
                                        Background="#FFF3E0"
                                        VerticalAlignment="Center">
                                    <Border.Visibility>
                                        <MultiBinding Converter="{StaticResource NotesIndicatorVisibilityConverter}">
                                            <Binding Path="Notes"/>
                                            <Binding Path="Payee.Notes"/>
                                        </MultiBinding>
                                    </Border.Visibility>
                                    <Border.ToolTip>
                                        <TextBlock TextWrapping="Wrap" Width="260">
                                            <TextBlock.Text>
                                                <MultiBinding Converter="{StaticResource NotesTooltipConverter}">
                                                    <Binding Path="Notes"/>
                                                    <Binding Path="Payee.Notes"/>
                                                </MultiBinding>
                                            </TextBlock.Text>
                                        </TextBlock>
                                    </Border.ToolTip>
                                    <Image Source="/BillApp;component/Assets/note-icon.png"
                                           Width="12"
                                           Height="12"
                                           Stretch="Uniform"/>
                                </Border>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Amount Due -->
                <DataGridTextColumn Header="Due" Binding="{Binding AmountDue, StringFormat=C, UpdateSourceTrigger=PropertyChanged}" Width="80">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Setter Property="Padding" Value="5,0"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- Amount Paid -->
                <DataGridTextColumn Header="Paid" Binding="{Binding AmountPaid, StringFormat=C, UpdateSourceTrigger=PropertyChanged}" Width="80">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Setter Property="Padding" Value="5,0"/>
                            <Setter Property="Foreground" Value="#4CAF50"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- Balance (only shown for financial accounts) -->
                <DataGridTemplateColumn Header="Balance" Width="80" SortMemberPath="Balance">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock HorizontalAlignment="Right" Padding="5,0">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="{Binding Balance, StringFormat=C}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Payee.IsAccount}" Value="False">
                                                <Setter Property="Text" Value=""/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Due Date -->
                <DataGridTemplateColumn Header="Due Date" Width="100" SortMemberPath="DueDate">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="5,0,0,0">
                                <TextBlock Text="{Binding DueDate, StringFormat=d}" VerticalAlignment="Center"/>
                                <Border Margin="6,0,0,0"
                                        Width="14"
                                        Height="14"
                                        CornerRadius="7"
                                        Background="#E8F5E9"
                                        BorderBrush="#66BB6A"
                                        BorderThickness="1"
                                        VerticalAlignment="Center"
                                        ToolTip="Autopay"
                                        Visibility="{Binding Payee.IsAutopay, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <TextBlock Text="A"
                                               FontSize="9"
                                               FontWeight="Bold"
                                               Foreground="#2E7D32"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                                </Border>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Paid Date -->
                <DataGridTemplateColumn Header="Paid Date" Width="100" SortMemberPath="PaidDate">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding PaidDate, StringFormat=d}" Padding="5,0"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Category -->
                <DataGridTemplateColumn Header="Category" Width="110" SortMemberPath="Payee.Category.Name">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Payee.Category.Name}" Padding="5,0"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Status with color coding -->
                <DataGridTemplateColumn Header="Status" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Padding="5,2" CornerRadius="3" HorizontalAlignment="Left">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#E0E0E0"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Status}" Value="Paid">
                                                <Setter Property="Background" Value="#C8E6C9"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Pending">
                                                <Setter Property="Background" Value="#FFF9C4"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Overdue">
                                                <Setter Property="Background" Value="#FFCDD2"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsOverdue}" Value="True">
                                                <Setter Property="Background" Value="#FFCDD2"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="{Binding Status}" FontSize="12" Foreground="#333333"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Frequency -->
                <DataGridTemplateColumn Header="Frequency" Width="110">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Frequency}" Padding="5,0"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Actions -->
                <DataGridTemplateColumn Header="Actions" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Edit"
                                        Command="{Binding DataContext.EditBillCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Padding="8,4"
                                        Margin="0,0,5,0"
                                        Cursor="Hand"/>

                                <Button Content="Pay"
                                        Command="{Binding DataContext.MarkAsPaidCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Padding="8,4"
                                        Margin="0,0,5,0"
                                        Background="#4CAF50"
                                        Foreground="White"
                                        BorderThickness="0"
                                        Cursor="Hand"
                                        Visibility="{Binding Status, Converter={StaticResource PaidVisibilityConverter}}"/>

                                <Button Content="New"
                                        Command="{Binding DataContext.CreateNewBillFromExistingCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Padding="8,4"
                                        Margin="0,0,5,0"
                                        Background="#2196F3"
                                        Foreground="White"
                                        BorderThickness="0"
                                        Cursor="Hand"
                                        ToolTip="Create a new bill based on this one"
                                        Visibility="{Binding Status, Converter={StaticResource ShowForPaidConverter}}"/>

                                <Button Content="Delete"
                                        Command="{Binding DataContext.DeleteBillCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Padding="8,4"
                                        Foreground="#F44336"
                                        Cursor="Hand"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>

        <DataGrid.RowStyle>
            <Style TargetType="DataGridRow">
                <Setter Property="Background" Value="{Binding ColorCode}"/>
                <Style.Triggers>
                    <MultiDataTrigger>
                        <MultiDataTrigger.Conditions>
                            <Condition Binding="{Binding IsOverdue}" Value="True"/>
                            <Condition Binding="{Binding ColorCode}" Value="{x:Null}"/>
                        </MultiDataTrigger.Conditions>
                        <Setter Property="Background" Value="#FFF3E0"/>
                    </MultiDataTrigger>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#E3F2FD"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
            </DataGrid.RowStyle>
        </DataGrid>

        <!-- Loading indicator -->
        <Border Grid.Row="2"
                Background="#80FFFFFF"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <TextBlock Text="Loading..."
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="16"/>
        </Border>

        <!-- Empty state -->
        <TextBlock Grid.Row="2"
                   Text="No bills found. Click '+ Add Bill' to create your first bill."
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Foreground="Gray"
                   FontSize="14">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding Bills.Count}" Value="0"/>
                                <Condition Binding="{Binding IsLoading}" Value="False"/>
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Visibility" Value="Visible"/>
                        </MultiDataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
    </Grid>
</UserControl>
