<UserControl x:Class="BillApp.Views.Reports.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:BillApp.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:ReportsViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{Binding Title}"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <!-- Available Reports -->
        <StackPanel Grid.Row="1">
            <TextBlock Text="Available Reports"
                       FontSize="16"
                       FontWeight="SemiBold"
                       Margin="0,0,0,15"/>

            <!-- Account Summary Report Card -->
            <Border Background="#F5F5F5"
                    CornerRadius="4"
                    Padding="15"
                    Margin="0,0,0,10"
                    MaxWidth="400"
                    HorizontalAlignment="Left">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Account Summary"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,5"/>
                        <TextBlock Text="View account details, credentials, and payment history. Printable A5 format."
                                   FontSize="12"
                                   Foreground="Gray"
                                   TextWrapping="Wrap"/>
                    </StackPanel>

                    <Button Grid.Column="1"
                            Content="Open"
                            Command="{Binding OpenAccountSummaryReportCommand}"
                            Padding="15,8"
                            Margin="15,0,0,0"
                            VerticalAlignment="Center"
                            Background="#1976D2"
                            Foreground="White"/>
                </Grid>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
